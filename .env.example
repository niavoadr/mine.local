DB_HOST=localhost
DB_PORT=[port_de_la_base_de_données]
DB_USER=[nom_d'utilisateur]
DB_PASS=[mot_de_passe]
DB_NAME=[nom_de_la_base_de_données]

# Secret partagé pour l'authentification MAC (RADIUS MAB)
# Généré avec: openssl rand -base64 32
RADIUS_MAC_SECRET=
